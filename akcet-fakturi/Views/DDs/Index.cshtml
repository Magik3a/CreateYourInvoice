@using akcet_fakturi
@using Kendo.Mvc.UI
@model IEnumerable<akcetDB.DD>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewBag.Dds = "active";
}

<h2>ДДС</h2>
  <!-- Helper for info boxes after manipulating data -->
@ResultHelper.ResultFromTempData(TempData)
<p>
    @Html.ActionLink("Създай ново", "Create")
</p>


@(Html.Kendo().Grid(Model).Name("DDs").Sortable().Filterable().Pageable().Columns(columns =>
{
    columns.Bound(m => m.Value);
    columns.Bound(m => m.DateCreated);
    columns.Bound(m => m.DateModified);
    columns.Bound(m => m.UserName);

    columns.Template(@<text>
        @Html.ActionLink("Редактирай", "Edit", new { id = item.DdsID }) |
        @Html.ActionLink("Детайли", "Details", new { id = item.DdsID }) |
        @Html.ActionLink("Изтрии", "Delete", new { id = item.DdsID })
    </text>).Title("Функции");
})
      )
